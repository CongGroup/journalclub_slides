## Installation

```bash
npm install -g reveal-md
```

---------------

## Local test

Enable option `-w` for live reload of presentation whenever changes on markdown file are maded.

```
reveal-md content/[your_presentation_file].md -w
```

-------------

## Build static html files

Run the `convertstatic.sh` script.